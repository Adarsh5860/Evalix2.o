# Port for backend Express server (default: 5001)
PORT=5001

# Optional: Google Gemini API Key for semantic embeddings
# If omitted, local character-frequency fallback embeddings are used
# GEMINI_API_KEY=your_gemini_api_key_here
